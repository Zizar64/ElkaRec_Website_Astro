---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

export async function getStaticPaths() {
  const blogPosts = await getCollection('blog');
  return blogPosts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();

function formatDate(date: Date) {
  return new Intl.DateTimeFormat('fr-FR', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  }).format(date);
}
---

<Layout title={`${post.data.title} - Blog ElkaRec`} description={post.data.description} image={post.data.image}>
  <Header />

  <main class="pt-24 pb-20">
    <!-- Article Header -->
    <article class="container mx-auto px-6 py-12">
      <div class="max-w-4xl mx-auto">
        <!-- Breadcrumb -->
        <nav class="flex items-center gap-2 text-sm text-zinc-500 mb-8">
          <a href="/" class="hover:text-red-400 transition-colors">Accueil</a>
          <span>/</span>
          <a href="/blog" class="hover:text-red-400 transition-colors">Blog</a>
          <span>/</span>
          <span class="text-zinc-400">{post.data.title}</span>
        </nav>

        <!-- Article Title -->
        <header class="mb-12">
          <h1 class="text-4xl md:text-5xl font-bold text-white mb-6 leading-tight">
            {post.data.title}
          </h1>

          <!-- Featured Image -->
          {post.data.image && (
            <div class="mb-8 rounded-xl overflow-hidden border border-zinc-800 max-w-2xl mx-auto">
              <img
                src={post.data.image}
                alt={post.data.title}
                width="1200"
                height="630"
                class="w-full h-auto object-cover"
                loading="eager"
              />
            </div>
          )}

          <!-- Meta Info -->
          <div class="flex flex-wrap items-center gap-4 text-zinc-400 mb-6">
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
              <time datetime={post.data.pubDate.toISOString()}>
                {formatDate(post.data.pubDate)}
              </time>
            </div>

            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
              <span>{post.data.author}</span>
            </div>
          </div>

          <!-- Tags -->
          <div class="flex flex-wrap gap-2">
            {post.data.tags.map((tag) => (
              <span class="px-4 py-2 bg-red-500/10 border border-red-500/20 text-red-400 text-sm rounded-lg">
                {tag}
              </span>
            ))}
          </div>
        </header>

        <!-- Article Content -->
        <div class="prose prose-invert prose-lg max-w-none
                    prose-headings:text-white prose-headings:font-bold
                    prose-h1:text-3xl prose-h1:mb-6 prose-h1:border-b prose-h1:border-red-500/20 prose-h1:pb-4
                    prose-h2:text-2xl prose-h2:mb-5 prose-h2:mt-12 prose-h2:text-red-400 prose-h2:border-l-4 prose-h2:border-red-500 prose-h2:pl-4 prose-h2:-ml-5
                    prose-h3:text-xl prose-h3:mb-4 prose-h3:mt-8 prose-h3:text-zinc-100 prose-h3:font-semibold
                    prose-h4:text-lg prose-h4:mb-3 prose-h4:mt-6 prose-h4:text-red-400/80 prose-h4:font-semibold
                    prose-p:text-zinc-300 prose-p:leading-relaxed prose-p:text-base prose-p:mb-6 prose-p:text-justify
                    prose-a:text-red-400 prose-a:font-medium prose-a:underline prose-a:decoration-red-500/30 prose-a:underline-offset-4 prose-a:decoration-2 hover:prose-a:text-red-300 hover:prose-a:decoration-red-400/50 prose-a:transition-all
                    prose-strong:text-white prose-strong:font-bold prose-strong:bg-gradient-to-r prose-strong:from-red-500/10 prose-strong:to-transparent prose-strong:px-1.5 prose-strong:py-0.5 prose-strong:rounded
                    prose-em:text-zinc-200 prose-em:italic
                    prose-ul:text-zinc-300 prose-ul:my-6 prose-ul:space-y-3
                    prose-ol:text-zinc-300 prose-ol:my-6 prose-ol:space-y-3
                    prose-li:text-zinc-300 prose-li:leading-relaxed prose-li:text-justify
                    prose-li:marker:text-red-400 prose-li:marker:font-bold
                    prose-code:text-red-300 prose-code:bg-zinc-900/80 prose-code:px-2 prose-code:py-1 prose-code:rounded prose-code:font-mono prose-code:text-sm prose-code:border prose-code:border-red-500/20
                    prose-pre:bg-zinc-900/80 prose-pre:border prose-pre:border-zinc-700 prose-pre:rounded-xl prose-pre:p-6
                    prose-blockquote:border-l-4 prose-blockquote:border-red-500 prose-blockquote:bg-red-500/5 prose-blockquote:text-zinc-300 prose-blockquote:pl-6 prose-blockquote:pr-6 prose-blockquote:py-4 prose-blockquote:rounded-r-lg prose-blockquote:my-8 prose-blockquote:text-justify
                    prose-img:rounded-xl prose-img:border prose-img:border-zinc-700 prose-img:shadow-2xl">
          <Content />
        </div>

        <!-- Article Footer -->
        <footer class="mt-16 pt-8 border-t border-zinc-800">
          <div class="flex items-center justify-between">
            <a href="/blog"
               class="flex items-center gap-2 text-red-400 hover:text-red-300 transition-colors">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
              </svg>
              Retour au blog
            </a>

            <!-- Share buttons could go here -->
          </div>
        </footer>
      </div>
    </article>

    <!-- Related Posts CTA -->
    <section class="container mx-auto px-6 py-16">
      <div class="max-w-4xl mx-auto text-center bg-gradient-to-br from-red-900/20 to-zinc-900/50 p-12 rounded-2xl border border-red-500/20">
        <h2 class="text-3xl font-bold text-white mb-4">Vous avez aimé cet article ?</h2>
        <p class="text-zinc-300 mb-8">
          Découvrez nos autres articles et suivez-nous pour ne rien manquer
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/blog"
             class="px-8 py-4 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-lg transition-all transform hover:scale-105">
            Voir tous les articles
          </a>
          <a href="/contact"
             class="px-8 py-4 bg-zinc-800 hover:bg-zinc-700 text-white font-semibold rounded-lg transition-all border border-zinc-700">
            Nous contacter
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>
